<%#
name: RanjanRestart Services - Katello SSH Default
snippet: false
template_inputs:
- name: helper
  required: true
  input_type: user
  description: A comma separated list of commands to run to restart services
  advanced: false
  value_type: plain
model: JobTemplate
job_category: Katello
description_format: Restart Services
provider_type: SSH
kind: job_template
organizations:
- Default Organization
locations:
- Default Location
%>

<%= input(:helper).split(",").map {|split| "#{split}" }.join("\n") %>

katello-tracer-upload